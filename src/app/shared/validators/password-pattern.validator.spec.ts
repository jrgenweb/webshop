import { PasswordPattern } from './password-patternvalidator';

describe('PasswordPattern', () => {
  it('should create an instance', () => {
    expect(new PasswordPattern()).toBeTruthy();
  });
});
